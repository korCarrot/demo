spring:
  datasource:
    driver-class-name: org.h2.Driver # Database 를 H2 로 설정
    url: jdbc:h2:mem:testdb
    username: sa

jpa:
  hibernate:
    ddl-auto: create-drop
    show-sql: true

h2:
  console:
    enabled: true
    settings:
      web-allow-others: true